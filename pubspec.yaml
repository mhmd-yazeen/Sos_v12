name: sos_v12
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"



dependencies:
  flutter:
    sdk: flutter
  sensors_plus: ^6.1.1
  cupertino_icons: ^1.0.8
  audioplayers: ^6.3.0
  torch_light: ^1.1.0
  http: ^1.3.0
  simple_ripple_animation: ^0.1.0
  geolocator: ^9.0.0
  workmanager: ^2.0.0
  permission_handler: ^11.4.0


  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/sounds/beep1s.mp3
    - assets/sounds/beep3s.mp3
  uses-material-design: true
